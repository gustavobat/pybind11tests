# - Config file for the PointLib package
# It defines the following variables
#  PointLib_INCLUDE_DIRS - include directories for using PointLib
#  PointLib_LIBRARIES    - PointLib library to link against

# In the future, if project executables are to be installed, their names should also be defined here:
#  <exec01_name>_EXECUTABLE   
#  <exec02_name>_EXECUTABLE  
#  ...
#
# so that they can be used below
 
# Compute paths
get_filename_component(PointLib_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(PointLib_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")
 
# These are IMPORTED targets created by PointLibTargets.cmake
set(PointLib_LIBRARIES PointLib)

